# Renders the "Sponsor" button at the top of the repo page and in the sidebar.
#
# GitHub only reads this file from the DEFAULT branch (main), so a copy sitting
# only on Rawhide would do nothing.
#
# Ko-fi is the same destination the app already links to, in the sidebar and in
# the update dialog — one place to support the project, not three.
ko_fi: sosukeaizen37411

# GitHub Sponsors is not set up on this account yet (checked 2026-08-02:
# hasSponsorsListing = false). An entry here for a non-existent listing renders
# nothing, so it stays commented out. To turn it on: enable a listing at
# https://github.com/sponsors, then uncomment the line below.
# github: [linux4life1]
